name = "argparse"
version = "0.1.0"
description = "CLI argument parsing for Donna"
licences = ["MIT"]
repository = { type = "github", user = "donna-lang", repo = "argparse" }
links = [{ title = "Docs", href = "https://donna-lang.github.io/argparse/" }]

[dependencies]
donna = { git = "https://github.com/donna-lang/donna_stdlib", version = ">=0.1.0 and <1.0.0" }

[dev-dependencies]
unittest = { git = "https://github.com/donna-lang/unittest", version = ">=0.1.0 and <1.0.0" }
